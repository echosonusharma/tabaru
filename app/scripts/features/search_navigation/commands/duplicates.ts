import { CommandMeta } from "./types";

export const duplicatesMeta: CommandMeta = {
  key: "d",
  label: "Close duplicates",
  description: "Close duplicate tabs across all windows",
  requiresKeyword: false,
};
